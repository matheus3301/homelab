apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-world
  namespace: hello-world
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: hello-world
      port:
        number: 80
  tls:
    - hosts:
        - hello-world
